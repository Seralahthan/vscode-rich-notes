# Marketplace screenshots

Drop the following PNGs here (referenced by the top-level README via GitHub raw
URLs). The repository must be **public** for these to render on the Marketplace.

- `hero.png` — the rich-text editor showing a well-formatted note
- `editing.png` — the slash menu open and/or the formatting toolbar
- `notion-sync.png` — Rich Notes and the same Notion page side by side
- `conflict.png` — the conflict diff with the resolve toolbar buttons
